transformers==4.41.2     # Hugging Face Transformers (for GPT-2 model)
torch>=2.0.0             # PyTorch (for running models)
tqdm                     # For progress bars during training/inference
scikit-learn             # For evaluation metrics
pandas                   # For loading/saving data
numpy                    # For numerical operations
